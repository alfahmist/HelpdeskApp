CREATE PROCEDURE [dbo].[SP_LoginClient]
	@email varchar(200)
AS
BEGIN
			Select cl.[Name], cl.Email, acc.[Password]
			FROM dbo.TB_M_Client  AS cl
			JOIN dbo.TB_M_AccountClient AS acc ON cl.ID = acc.ID
			WHERE cl.Email = @email

END;